apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-oncall-pi.fullname" . }}
  labels:
    {{- include "grafana-oncall-pi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-oncall-pi.selectorLabels" . | nindent 4 }}
