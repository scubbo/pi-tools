apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grafana-oncall-pi.fullname" . }}-test-connection"
  labels:
    {{- include "grafana-oncall-pi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grafana-oncall-pi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
